
#include "SurfGpuTracker.h"

namespace gf{

    void SurfGpuTracker::detectKeyPoints(const cv::Mat &sourceImg, Vector<cv::KeyPoint> &dstKeyPoints) {

    }

    void SurfGpuTracker::computeDescriptors(const cv::Mat &sourceImg, Vector<cv::KeyPoint> &keyPoints,
                                            cv::OutputArray &descriptors) {

    }

    void SurfGpuTracker::track(const cv::Mat &img1, const cv::Mat &img2,cv::Mat& currentPose) {

    }

    void SurfGpuTracker::detectAndComputeDescriptors(const cv::Mat &sourceImg, Vector<cv::KeyPoint> &keyPoints,
                                                     cv::OutputArray &descriptors) {

    }
}
