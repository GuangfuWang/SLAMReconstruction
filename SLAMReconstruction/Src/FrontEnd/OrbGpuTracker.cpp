
#include "OrbGpuTracker.h"

namespace gf{

    void OrbGpuTracker::detectKeyPoints(const cv::Mat &sourceImg, Vector<cv::KeyPoint> &dstKeyPoints) {

    }

    void OrbGpuTracker::computeDescriptors(const cv::Mat &sourceImg, Vector<cv::KeyPoint> &keyPoints,
                                           cv::OutputArray &descriptors) {

    }

    void OrbGpuTracker::track(const cv::Mat &img1, const cv::Mat &img2,cv::Mat& currentPose) {

    }

    void OrbGpuTracker::detectAndComputeDescriptors(const cv::Mat &sourceImg, Vector<cv::KeyPoint> &keyPoints,
                                                    cv::OutputArray &descriptors) {

    }
}
